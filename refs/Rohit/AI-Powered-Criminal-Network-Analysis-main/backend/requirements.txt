# ============================================
# NCRB Criminal Network Analysis System
# Python dependencies — embedded deployment
#
# Deliberately minimal. No Docker, no Neo4j driver, no Elasticsearch client,
# no torch/transformers. Every dependency here is used; the platform runs on
# SQLite + FTS5 + NetworkX in a single process.
# ============================================

# --- API framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4

# --- Graph analytics ---
networkx==3.4.2
numpy==2.2.1
scipy==1.15.1

# --- Fuzzy matching for entity resolution ---
rapidfuzz==3.11.0

# --- Utilities ---
python-dateutil==2.9.0.post0

# --- Testing ---
pytest==8.3.4
httpx==0.28.1
